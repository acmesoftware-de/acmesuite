package de.acmesoftware.acmesuite.org.domain;

/** Where an employee primarily works — drives the Team roster status (remote vs. on-site). */
public enum WorkLocation {
    ONSITE,
    REMOTE,
    HYBRID
}
