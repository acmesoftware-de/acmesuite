import type { ThemeMeta } from '../../src/theme/types'

export const paperMeta: ThemeMeta = {
  id: 'paper',
  label: 'Paper',
  description: 'Warm, rounded, editorial — a proof that the whole look is theme-owned.',
  modes: ['dark', 'light'],
  fonts: { display: 'Archivo', ui: 'Archivo', mono: 'Space Mono' },
}
